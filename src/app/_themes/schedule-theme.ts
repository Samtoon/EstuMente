import { createTheme } from "@mui/material";

export const scheduleTheme = createTheme({
  palette: {
    primary: {
      main: "#CC3333",
    },
    secondary: {
      main: "#C2BABA",
    },
  },
});
