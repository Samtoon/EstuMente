import { createTheme } from "@mui/material";

export const scheduleTheme = createTheme({
    palette: {
        primary: {
          main: "#7F00FF"
        },
        secondary: {
          main: "#f5f5f5"
        },
      },
})